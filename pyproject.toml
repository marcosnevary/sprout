[project]
name = "plant-watch"
version = "0.1.0"
description = "Automated plant monitoring system that records videos and collects soil moisture data to track plant conditions."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
]
